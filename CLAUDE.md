# claude-commander

Native macOS command center for Claude Code users — tasks, plans, sessions, git, env vars, and deploy config.

## Context Loading

At session start, load from the context vault:

- `get_context(tags: ["claude-commander"])` — project architecture, decisions, patterns
